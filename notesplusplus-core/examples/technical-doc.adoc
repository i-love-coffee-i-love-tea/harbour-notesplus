= Technical Documentation
:toc:

== Overview

This document describes the architecture of the Notes++ application.

== Architecture

Notes++ uses a 3-crate workspace:

1. `notesplusplus-core` — Pure Rust core library
2. `notesplusplus-sailfish` — Sailfish OS GUI
3. `vendor/sailors` — SailfishApp FFI

[NOTE]
====
The core library has no Qt or Sailfish dependencies and can be tested independently with `cargo test -p notesplusplus-core`.
====

== Data Model

=== Storage

* *Files*: `~/.local/share/harbour-notesplusplus/notes/*.adoc`
* *Database*: `~/.local/share/harbour-notesplusplus/notesplusplus.db`

=== SQLite Schema

[source,sql]
----
CREATE TABLE pages (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    filename TEXT NOT NULL UNIQUE,
    title TEXT NOT NULL,
    is_journal INTEGER NOT NULL DEFAULT 0,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL,
    block_count INTEGER NOT NULL DEFAULT 0
);
----

== API Reference

=== Block Types

[cols="1,2"]
|===
| Type | Description

| Heading
| Level 1-6 headings

| Paragraph
| Multi-line text blocks

| CodeBlock
| Delimited code blocks

| Table
| AsciiDoc tables
|===

[WARNING]
====
Always run `cargo test -p notesplusplus-core` before committing changes.
====
