= ADR-002: Plain-Text AsciiDoc Files on the Filesystem as Canonical Storage
:status: Accepted
:date: 2026-09-01
:toc:
:icons: font
:sectnums:

[NOTE]
.Status: Accepted
Notes Plus stores all documents as plain-text AsciiDoc (`.adoc`) files on the local filesystem, avoiding proprietary databases and cloud lock-in.

== Context

Note-taking applications frequently store user notes in monolithic SQLite databases, proprietary binary formats, or vendor-locked cloud backends. When storage schemas change or applications are abandoned, users face data lock-in and high migration friction.

Furthermore, mobile device file synchronization (via Syncthing, Nextcloud, or Git) requires granular file-level conflict resolution.

== Decision

Store all user notes as standard, human-readable AsciiDoc (`.adoc`) files directly on the local filesystem in the user's document directory (`~/Documents/Notes Plus` or standard XDG document paths).

=== Feature Comparison: AsciiDoc vs. Markdown

AsciiDoc was selected over standard Markdown due to its rich semantic markup capabilities:

[cols="1,2,2", options="header"]
|===
| Feature | AsciiDoc | Standard Markdown

| *Admonitions*
| Native syntax (`NOTE:`, `TIP:`, `IMPORTANT:`, `WARNING:`, `CAUTION:`)
| Non-standard blockquote hacks (`> [!NOTE]`)

| *Tables*
| Multi-column, alignments, widths (`[cols="2,1"]`, `[cols="3*"]`), cell spans
| Basic pipe tables without colspans/rowspans

| *Delimited Blocks*
| Typed fences: `----` (code), `****` (sidebar), `____` (quote), `....` (literal)
| Only code fences (`````)

| *Attributes & Metadata*
| Document attributes (`:toc:`, `:icons: font`, `:sectnums:`)
| Non-standard frontmatter (YAML/TOML)

| *UI Macros*
| Keyboard (`kbd:[Ctrl+S]`), Button (`btn:[Save]`), Menu (`menu:File[Save]`)
| None / raw HTML
|===

== Storage Structure

[source,ascii]
----
~/Documents/Notes Plus/
├── Meeting_Notes.adoc           // Top-level note
├── Technical_Guide.adoc         // Top-level note
└── Notes_Plus_Documentation/    // Group directory // <1>
    └── ADRs/                    // Subgroup directory // <2>
        ├── 001-hybrid-core-and-gui-architecture.adoc
        └── 002-asciidoc-as-canonical-storage-format.adoc
----
<1> Directory without same-named `.adoc` file represents a note group.
<2> Nested directory represents a subgroup.

== Consequences

=== Positive / Utility Delivered

* icon:unlock[] *Zero Vendor Lock-In* -- Notes remain portable plain-text files that can be viewed, edited, or backed up with any text editor or CLI tool.
* icon:refresh[] *Transparent Multi-Device Sync* -- Works out of the box with file synchronization engines (Syncthing, Nextcloud, Git, rsync) without database merge conflicts.
* icon:file-text[] *Rich Semantic Markup* -- Provides structured technical documentation capabilities beyond standard Markdown without external plugins.

=== Trade-offs & Mitigations

[IMPORTANT]
.Search Performance on Mobile Storage
Full-text search cannot rely on scanning every plain-text file on demand without mobile I/O latency. This is resolved by maintaining an asynchronous derived SQLite FTS5 search index (see xref:004-sqlite-fts5-as-derived-search-cache.adoc[ADR-004]).

== Related Decisions

* xref:003-ast-driven-parsing-and-granular-block-mutation.adoc[ADR-003: AST-Based Block Parsing Pipeline]
* xref:004-sqlite-fts5-as-derived-search-cache.adoc[ADR-004: SQLite FTS5 Search Cache]
* xref:010-note-storage-directory-convention.adoc[ADR-010: Note Storage Directory Convention]
